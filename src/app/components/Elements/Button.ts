/**
 * Button Element
 * This will extends ButtonStyle
 */
import styled from 'styled-components/macro';
import ButtonStyle from './ButtonStyle';

const Button = styled.button`
  ${ButtonStyle};
`;

export default Button;
