import styled from 'styled-components/macro';

const Wrapper = styled.footer`
  text-align: center;
  padding: 20px;
  font-size: 0.9em;

  p {
    margin: 0 0;
    color: #a9b1b8;
  }
`;

export default Wrapper;
